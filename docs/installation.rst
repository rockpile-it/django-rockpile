============
Installation
============

At the command line::

    $ easy_install django-rockpile

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rockpile
    $ pip install django-rockpile